{
  "name": "@sre/core",
  "version": "0.1.0",
  "type": "module",
  "description": "Framework-agnostic speculative-execution engine. Zero DOM, zero React.",
  "license": "MIT",
  "exports": {
    ".": "./src/index.ts",
    "./testing": "./src/testing.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^2.1.8"
  }
}
